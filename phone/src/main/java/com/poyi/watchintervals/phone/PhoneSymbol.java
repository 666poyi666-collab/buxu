package com.poyi.watchintervals.phone;

/**
 * Original phone-side symbols. These names describe product concepts rather than mapping to
 * platform-owned glyphs, so the same vector language can be rendered consistently everywhere.
 */
public enum PhoneSymbol {
    BRAND,
    PLAN,
    WORKOUT,
    HISTORY,
    SLEEP,
    BACK,
    LOCATION
}
