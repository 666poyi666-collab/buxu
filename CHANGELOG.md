# Changelog

本文件记录用户可感知的版本变化，格式参考 Keep a Changelog。手表端和手机端版本独立，在标题中分别标明。

## [Watch 0.23.0 / Phone 0.25.1] - 2026-08-30

### Added

- Cloud MCP 计划管理补齐单计划读取、移动安排到分组、替换完整阶段序列；计划、阶段、分组参数改为严格结构化 schema，ChatGPT 能直接创建/重命名/删除空分组，创建/修改/移动/删除安排并逐项修改跑步、快走、休息目标。
- 手表新增离线中文阶段语音：阶段开始播报序号、跑/走/休息和灵活距离/时间目标，时间阶段提前 5 秒、距离阶段提前 50 米预告；新增语音开关、清晰/沉稳/活力预设与试听页。
- 手表亮屏恢复新增悬浮训练返回层，用于绕过 OWW221 对第三方后台 Activity 和 full-screen notification 的厂商限制。
- 手机端连接与云同步设置显示云端状态徽章与链路说明：手表 → 手机 → 云端 → Cloud MCP → ChatGPT，明确电脑不参与运行时。
- 新增手机端可访问性契约层 `PhoneUiContract`，把阶段类型、目标单位、前移、后移、移除、目的地、计划行、历史行与睡眠阶段的中文朗读描述集中为无 Android 依赖的纯函数，并配回归测试。
- 新增手表端设计令牌 `WatchTokens`，集中色值、字号、间距与圆角语义，页面不再内联裸色值。
- 新增《Cloud MCP 与 ChatGPT 接入》文档，包含生产端点健康与 OAuth 元数据的实测结果、完整接入步骤与故障排查。
- 新增可重复创建的 API 35 手机 AVD 工具；双端 AVD 都会动态解析 Android SDK/模块版本，并把本包 Activity 实际前台可见作为 Verify 条件。

### Changed

- Phone 0.25.1 统一术语为“分组 / 安排 / 阶段”：编辑安排使用稳定 groupId 和现有分组选择器，不再靠可变分组名称隐式建组；“今天”补当前分组前后安排与真实同步状态。Watch 26 项大库改为“分组 → 安排 → 详情”三级浏览。
- 完整同步顺序改为 Cloud 权威库先落手机、随后同一快照投影 Watch；首次睡眠回填单次 exchange 从 25 项限制为 5 项，避免中国到生产 D1 的 20 秒请求超时。
- 手机端界面重写为 Compose + MVI：原先 1299 行的单体 `MainActivity` 拆分为 `PhoneViewModel` 与四个独立页面，计划三级导航由密封路由 `PlanRoute` 表达，状态收敛为单一 `PhoneUiState`，所有网络与磁盘访问在单并发 IO 作用域内串行执行。
- 手机端设计令牌集中管理：颜色、字号、间距、8dp 内容容器与四档操作层级（主／次／危险／幽灵）统一由 Compose 组件消费；底部导航改为贴底功能条，设置使用独立 16dp 容器。
- 品牌图标几何重建：启动器自适应前景、单色层与传统层共用同一“间歇路线”几何，折返转弯改用整数半径圆弧，传统层按 4/9 等比缩放以保证三种呈现完全一致。
- 手表训练阶段强调色改由 `Stage.Kind` 枚举判断，不再依赖本地化阶段名字符串。
- 手表端 12 处内联色值替换为语义令牌（浅色底、强调面板底、轨迹画布底与浮层遮罩）。
- 阶段倒计时页增加心率、累计距离和估算热量三列实时指标，与倒计时环保持同屏；缺失心率继续显示 `--`。
- 手机根层改为轻量连接事实带和图标同步/设置；计划页去除卡片套卡片，训练页同屏呈现阶段、时间、距离、当前配速、心率、热量、步数与平均心率。
- 手表主页用全宽“开始训练”替代发光圆形按钮；默认训练页同屏增加本阶段剩余值。
- 手机连接事实带改为整行可点击并增加进入提示；连接设置按可用高度伸缩，同时避让键盘与系统导航栏。
- 计划分组只保留“新增”和“更多”两个行内入口，重命名/删除收进菜单；阶段类型与目标单位改为显式分段选择，类型变化不再清掉同单位目标值。
- 手机视觉从“浮动玻璃卡”回炉为克制的数据工具：页面标题统一为 24sp，容器 8dp，品牌红为通用主色，底栏 60dp 贴底，计划选中态取消粉底绿边，训练页移除装饰圆环。
- 手机连接设置默认只显示连接与云状态，LAN、endpoint 和 token 收入按需展开的高级区；Watch 去除发光训练标记，将卡片、按钮和确认层统一为 7–10dp 紧凑圆角与低饱和运动色。
- Watch 首页信息架构改为固定“步序”品牌、动态运动类型、当前计划和“本次／本周”双仪表格；消除计划摘要与主操作之间的空洞，“选择训练安排”改为更直接的“更换计划”。
- 运动开始链路改为 Service 绝对 3 秒倒计时；点击后不等待 GPS，首 fix 每 15 秒退避重试，训练中/暂停 GPS cadence 降到 2 秒/10 秒，准备态不持有长时 wakelock，避免倒计时漂移与无谓耗电。
- Phone 根层取消独立连接卡，BLE/LAN 状态并入品牌顶栏；底部目的地改为深色控制条，当前计划与训练实时数据使用深色性能面板。Watch 当前计划进入独立面板并新增按阶段类型着色的时间线色带。
- Phone 第一目的地从“计划管理”改为“今天”：默认只显示当前训练、阶段顺序和训练控制入口，计划库通过“管理训练计划”按需进入；四个目的地改为“今天／训练／记录／恢复”。Watch 当前计划面板整块可点击更换计划。
- Watch 全部主要命令统一为原创 PLAY/PAUSE/STOP/LIST/BACK/FORWARD/DELETE/CHECK/HISTORY Drawable；准备页删除发光圆钮，改为来源状态格与全宽开始/取消；训练控制删除 Unicode 圆控件，改为双列暂停/结束动作条。Phone Button 增加共享矢量图标槽，训练、计划、保存、删除、同步和 Cloud 操作统一图标；旧 Java 训练详情同步为 8dp/0 elevation 与深色系统导航。
- Watch 准备页改为中央定位进度仪表；训练默认落到阶段倒计时页，控制页左移一屏；训练不再强制常亮。Phone“今天”补阶段色带、训练实时摘要、最近训练指标和记录入口。
- GPS cadence 细分为准备搜星 1 秒、实时锁定后 5 秒、训练 2 秒和暂停 10 秒；暂停取消单次 fix 并禁止重试，恢复后再获取。
- 手机在 1.6× 以上字体使用横向“图标+短标签”底栏和精简连接状态；计划详情/编辑器隐藏全局底栏，表单空间与草稿返回层级更明确。

### Fixed

- 修复 Cloud device token 在签名迁移后缺失，却仍把“ChatGPT 已连接”当作设备链路可用的问题；手机在首屏和设置页明确显示云端阻断。本轮重新 provision 正式 token 后，Cloud、Phone、Watch 实际收敛到 revision 40（BUG-074）。
- 修复安排编辑只保存分组名称、重命名后重新归类；修复删除非空分组会偷偷创建“我的计划”并搬移内容；修复删除不存在安排仍推进整个库 revision；修复新建空分组被 UI 过滤、无法管理。非空分组现在拒绝删除，单项删除只影响精确 planId（BUG-075）。
- 手机主界面 UI 构建与 NSD 发现、同步编排、权限、计划 CRUD 全部耦合在一个 Activity 内，计划三级导航靠手写可见性状态机，容易漏配（BUG-058）。
- 手表训练页用 `stageName.equals("快走")` 判断阶段类型，阶段文案一旦调整配色即静默失效（BUG-059）。
- 手表端语义色值在三个页面重复内联，同一语义无法保证一致（BUG-060）。
- 手机端可访问性测试读取 `MainActivity.java` 源码文本断言 `dp()` 字面量，既验证不了运行时行为，也会因界面重构而误报（BUG-061）。
- 手表训练在息屏后点亮可能停在表盘：前台服务现在按 `ACTION_SCREEN_OFF`/`ACTION_SCREEN_ON` 配对按准备/运行状态恢复对应界面，并对重复屏幕广播节流（BUG-052）。
- 手表阶段倒计时页只能看到剩余值，无法同时查看心率、累计距离和热量（BUG-063）。
- 修复双端首屏操作层级失焦、计划列表嵌套卡片和训练页装饰挤占数据的问题（BUG-065）。
- 修复重复 `connect()` 重启 BLE、LAN 已在线却被标为 BACKOFF，以及双端 API/BLE 服务恢复互相牵连的问题（BUG-066）。
- 修复手机连接条点击死区、设置层空白点击穿透、固定高度裁切、分组操作拥挤、阶段盲切和训练卡大字体裁字（BUG-067）。
- 修复 Watch ADB 保活过滤 `offline` 端点、在无 USB 时不执行重拨的问题；成功连接后新增 OWW221 型号复核（BUG-068，开发链路）。
- 修复 Watch 在心率/步数权限被拒绝后重复弹出授权的问题；缺权限时主按钮明确显示“授权并开始训练”，可选传感器拒绝后按缺失数据降级（BUG-069）。
- 修复手机已连接且手表空闲时训练页提示“点击下方按钮”却没有开始按钮的问题；成功响应与异常响应现在共用同一动作推导规则（BUG-070）。
- 修复 3/2/1/GO 由 Activity 相对延时持有、息屏会重置，以及准备/暂停定位与 wakelock 过度耗电的问题（BUG-071）。
- 修复 OWW221 亮屏后强制停在 Launcher，以及 `singleTop` 已恢复但“返回训练”overlay 未移除的问题；授予悬浮层权限后直接回到原训练仪表（BUG-072）。
- 修复阶段切换只有无语义短音、跑动中必须抬腕读取下一阶段的问题（BUG-073）。
- 修复无距离/步数但已完成时间阶段的有效训练被本周统计显示为“暂无记录”；此类记录现在按时长计入，短误触仍过滤（BUG-076）。
- 修复 Baidu 坐标转换 JNI 缺失时历史详情整页崩溃；地图不可用改为明确占位，统计、阶段与原始轨迹继续保留（BUG-077）。
- 修复 AVD Verify 只看 APK 安装或只接受 Watch 主页造成的假绿/假红（BUG-078，开发门禁）。

### Known Issues

- 仓库路径含非 ASCII 字符时单元测试无法加载，需在纯 ASCII 路径的 git 工作树中执行；编译与打包不受影响（BUG-062）。
- 旧 v0.22.0 设备/Release 签名链无法回滚覆盖；已按用户确认完成双端数据迁移并重新建立配对，后续构建需保持当前本机 debug keystore。Cloud V3 device token 已在 Phone 0.25.1 重新 provision（BUG-064/074）。

## [Watch 0.22.0 / Phone 0.24.0] - 2026-08-18

### Added

- 手机睡眠页新增 31 天本地离线投影、最近同步时间、评分/总时长双指标，以及深睡、浅睡、REM、清醒比例图；多段睡眠完整聚合，缺失字段保持未知而不补零。
- 手机睡眠页新增真实阶段时间线和近 7 晚总时长趋势：保留夜间清醒、多 session 空档与厂商未知阶段；手表 `/v1/sleep` 支持 7 天分页 coverage，避免 BLE 单响应过大。
- 新增可重复创建的 OWW221 API 30 等价 AVD 工具，固定 Android 11、378x496、320 dpi、60 Hz、竖屏和息屏参数，并明确 ColorOS/HealthKit/真实传感器仍必须真机验证。
- 手表训练阶段切换新增服务侧短促提示音与震动，界面只显示 1.8 秒、不可聚焦且不拦截横滑的轻量下一阶段提示。
- 新增 server-readable Cloud V3：Device Bearer Token 认证的 `/sync/v3/exchange`、持久 outbox/active request/cursor/receipt/conflict，以及 D1 计划、训练摘要、睡眠、实时状态和命令数据面。
- 手机前台新增 OkHttp WebSocket `/sync/v3/channel`；通道只接收 `sync_needed`，命令正文仍由轻量 exchange 拉取，断线和运行中补配置均自动重连。
- Cloud MCP 新增 `watch:read`、`watch:write`、`watch:control` 权限隔离，可读取真实计划、训练摘要/分段/聚合心率、睡眠和新鲜度，并创建计划或训练控制命令。
- 手表新增 `/v1/control/delete_workout` 幂等命令；相同 commandId 返回首次结果，不同正文复用 ID 拒绝，云端只在手表 ACK 后写训练 tombstone。

### Changed

- 手机默认视觉由深色候选切换为高对比日光亮色：浅灰画布、白色内容卡、半透明白色功能层和亮色系统栏覆盖四目的地、连接设置与历史详情；正文、次级文字、按钮和状态由自动化对比度门禁约束（`REQ-UI-006/011/013`）。
- Phone 与 Watch 启动器统一为同一组原创“间歇路线”矢量 path、颜色、背景和安全区，并由跨模块资源测试防止再次漂移；手表应用内训练标志改为更简洁的路线/前进几何（`REQ-UI-012`）。
- 建立长期维护与当次闭环规范：每次开工读取统一事实源，当前可解决的问题当次完成，Bug 修复记录根因并增加防复发测试，只有已举证的外部阻断或用户明确延期才允许保留未闭环项。
- 云端成为计划主版本，手机计划库改为离线缓存；首次 V3 可引导空云端，之后所有计划写入使用 expected revision，旧 revision conflict 保留本地 candidate 和服务器库。
- 业务数据不再做应用层 E2EE；HTTPS、安全 BLE、OAuth 和 Android Keystore device-token 包装继续保留。V2 源码/state 暂留迁移回退，但 Phone 0.24.0 不启用、不双写。
- 训练中每 10 秒、空闲每 60 秒上传实时状态；live/command exchange 不再重复扫描完整历史和 31 天睡眠。
- 原始轨迹、坐标和逐点心率固定为 local-only；云端允许完整计划、训练摘要/分段/聚合心率与睡眠 record/session/stage。
- 手机端重构为平台中立的 iOS/macOS 启发式功能层：内容仍使用实色数据卡，连接设置与四目的地底栏改为半透明浮层、同心圆角和实时 WindowInsets；系统栏统一亮色，页面标题与大字体安全区同步收敛（`REQ-UI-011`）。
- 手机底栏与历史详情不再使用 OEM 字体中的 Unicode 图标；新增原创 24×24 矢量 symbol 系统和中文可访问名称。Phone 启动器改为原创“间歇路线”自适应标志，并新增 Android 13 monochrome 主题图标层（`REQ-UI-012`）。
- 手机训练计划改为“列表 -> 详情 -> 编辑”三级结构；保存与“设为手表当前”职责分离，草稿支持 Activity 重建与放弃确认，重复阶段压缩展示，阶段类型和时间/距离切换使用安全默认值（`REQ-PLAN-007`）。

### Fixed

- 修复 Phone 睡眠页把当前连接当作数据可见性的硬前置；前台查看、手动同步和 Cloud V3 采集现在共用 `phone_sleep_cache`，断连、权限暂不可用、空响应或刷新失败均保留最后成功数据，损坏缓存安全降级（`BUG-051`）。
- 修复活动训练亮屏/点启动器后可能停在主页的问题；启动器、任务和通知统一路由到现有 `TrainingActivity`，不再由服务每秒抢前台，`WorkoutService` 仍是唯一训练状态所有者（`BUG-052`）。
- 修复手表训练结束/历史删除确认层仍可触摸或进入 TalkBack、历史详情右滑越级退出，以及 Service 晚绑定时结束动作丢失的问题；危险操作现在遵守确认层优先和详情到列表的返回层级（`BUG-053`）。
- 修复睡眠页、完整同步和 Worker 并发重复读取同一 31 天窗口的问题；三个入口现在共享进程级 single-flight，失败传播并在清槽后可重试（`BUG-054`）。
- 修复 LAN GET 失败时把“蓝牙可用”误当“安全会话已认证”、直接重放失败且延长请求 TTL 的问题；现在仅在原 TTL 内建立认证 BLE 回退，写请求不自动重放（`BUG-055`）。
- 修复计划编辑器部分操作只有 44dp 且 TalkBack 缺少阶段上下文的问题；保存、类型、移除和移动操作提升到 48dp，并朗读阶段序号与当前值（`BUG-056`）。
- 修复最近 31 天完整睡眠缓存未排除 Android 系统备份和设备迁移的问题；legacy backup、Android 12+ cloud backup 与 device transfer 现在全部显式排除该健康明细（`BUG-057`）。
- 修复双端 launcher 各自维护不同图形、手机端深色令牌与用户亮色偏好冲突的问题（`BUG-050`）。
- 修复手表 `/v1/history` 通过 `WorkoutRecord.fromJson()` 重建摘要时丢失已派生 `splits`、最佳配速与心率范围的问题；同步接口现在直接深拷贝 reconcile 后的摘要索引，并显式剔除轨迹、坐标集合和逐点心率（`BUG-043`）。
- 修复 WorkManager 与前台同步并发读写同一 V3 state、cursor ahead 无限重试、conflict 被误当 ACK、旧云端响应覆盖 HTTP 往返期间新本地编辑的问题。
- 修复命令成功后仅等待下一次 WorkManager 才回传 ACK 的延迟；现在同一次同步立即二次 exchange。手表离线不提前上报失败，30 秒过期后不再执行旧命令。
- 修复 WebSocket 未配置凭据时永不补连和 close/failure 重复安排 reconnect；`watch_cloud_v3.xml` 现同时排除 Auto Backup 与设备迁移。
- 修复厂商睡眠 OSA 负数哨兵被 V3 schema 拒绝，以及已成功上传睡眠仍长期保留旧 `local_schema_invalid` 大对象的问题。
- 修复计划 revision 把 device identity 误作 authority 的问题；成功 V3 响应现在携带服务端 owner/library `revisionDomainId`，Phone 原子保存 domain/revision/fingerprint，Watch 只允许 legacy→`v3d.*` 单向升级并拒绝跨 authority 回退，Worker 缺失/非法 domain 时 fail closed（`BUG-042`）。
- 修复 Phone→Watch 投影在首次失败、ACK-loss、journal 损坏、A→B→A、换表/重配或无互联网时不能可靠收敛的问题；同一 pending 保留 ID、新事件使用新 UUID，receipt 按 pairing generation 分域，journal 可重建，ACK 与删除同提交，独立无网 Worker 由 boot/连接/心跳/15 分钟周期补偿，网络 I/O 不再阻塞云命令（`BUG-044`）。
- 修复空云计划库在 Watch 被当成“缺少配置”而复活内置 1 km + 200 m 的问题；显式 empty marker 现在清除旧 profile、禁用主页开始并让远程 start 返回 `plan_unavailable`，选择新计划后可恢复（`BUG-045`）。
- 统一 Cloud 与 Phone 的计划 canonical fingerprint，保留 null selection/group 与非连续 sortOrder，避免相同云快照被周期性重复应用或在 Watch 改变语义（`BUG-046`）。
- 修复已绑定 `v3d.*` 的 Phone 仍接受缺失 domain 响应的问题；legacy fallback 现在只允许尚未绑定 authority 的旧在途响应。最终凭据复核与响应全部本地副作用也移入同一凭据锁，换 token/endpoint 后旧响应不能污染新 authority（`BUG-042`、`BUG-048`）。
- 修复 `lastAck=A + pending B + desired A` 会误清计划队列，以及本地删除把完整库发送为无效 `delete` 的问题；新的 A 必须使用新 ID，完整库删除统一为 `upsert`，旧 `delete` journal 自动升级（`BUG-044`）。
- 修复 Watch 先写空 library、后清旧 profile 的崩溃窗口；现在先从收到的 library materialize/clear profile，任一提交失败都不 ACK 并由同一 operationId 重试（`BUG-045`）。
- 修复 `watch_start_workout(planId)` 在 Phone 控制体中丢失目标、Watch 总启动当前计划的问题；同时将 start/pause/resume/toggle/stop 改为副作用前两阶段 command journal，toggle 重放固定为首次解析的 pause/resume（`BUG-002`、`BUG-049`）。
- 修复 Phone 0.23.0 设置仍展示 V2“加密云同步”、`/sync/v2/exchange`、恢复包和设备批准入口的问题；活动 UI 现在只配置 Cloud V3 与 Keystore device token，V2 源码/state 仅留迁移且不再提供调用入口（`BUG-047`）。

### Verification

- 前一正式 ChatGPT 候选已回读合成记录 `1200 m`、`2` 个分段（1000 m/300 s、200 m/60 s），随后通过产品命令删除并写 D1 tombstone；临时计划完成 Cloud→Phone→Watch 创建和反向删除，Phone/Watch revision 均为 4 且两类 outbox 为 0。本批 Watch/Phone JVM 44/87 项、双模块 lint/debug/release、Worker 10/5/8/39 项、Markdown 链接和 diff 门禁全绿，四个 APK 哈希记录在项目日志；目标 Watch 不在线，旧验收不冒充新 `revisionDomainId`/空库/ACK-loss/指定 planId/命令 journal 证据。
- 手机 UI 批次通过双模块 `test/lint/assembleDebug`（140 tasks）、MCP pytest 12/12、Markdown 本地链接和差异检查；API 35 模拟器验证 1.0×/2.0× 字体、深色启动画面、V3 设置、浮动底栏与四目的地可访问名称。厂商字体、多启动器蒙版和主题图标仍按 PT-026/027 留在发布真机门禁。
- 2026-08-18 当前候选门禁通过：`gradlew.bat test lint :app:assembleDebug :phone:assembleDebug --rerun-tasks --no-daemon --stacktrace` 成功，140 个 actionable tasks 全部通过；MCP `pytest mcp\\tests -q` 为 12 passed，`git diff --check` 和 Git 跟踪 Markdown 本地链接检查通过。
- APK 元数据：Watch `com.poyi.watchintervals` / `0.22.0` / versionCode `33`；Phone `com.poyi.watchintervals.phone` / `0.24.0` / versionCode `20`。两个 APK 均为 Android Debug 证书，证书 SHA-256 为 `7EB76B41EE20B76E877282F63D5468C016F09AED4513F5985F524ED325915FCD`。
- APK SHA-256：Watch `33C8D7974F12B72BC304E3594D2F15664483C639687666FB1CDCB62D0BC84F99`；Phone `6F084635091650231FAF5972013A7C76DCDBFD9CCC3246AEBB014824A836EB84`。
- 当前可下载候选发布为 GitHub [v0.22.0 prerelease](https://github.com/666poyi666-collab/WatchIntervals/releases/tag/v0.22.0)，资产包含两个 debug APK、`SHA-256.txt` 和 `build-info.json`。由于仍有 OWW221/Xiaomi 真机、户外传感器、Doze/重启和 Cloud V3 新版本门禁未完成，本版本不宣称正式稳定发布。

## [Watch 0.21.1 / Phone 0.22.1] - 未发布

### Added

- 手表主页新增「本周」训练量条：本周距离、次数与活动时间，全零测试记录不计入。
- 手表训练综合仪表和历史详情新增真实心率趋势图；只绘制传感器/历史实际样本，无数据时保持空白。
- 手机端新增 `SyncEnvelopeV1` 端到端加密双向同步：计划、计划库元数据与训练摘要使用 AES-256-GCM，支持持久 outbox/cursor、冲突双方留存、显式删除 tombstone、pull-first bootstrap 和 WorkManager 后台 catch-up。
- 新增加密密钥管理：device token/root 由 Android Keystore 包装；支持 PBKDF2-HMAC-SHA256 离线恢复包，以及绑定目标 deviceId、一次性 nonce、公钥指纹和 10 分钟有效期的已授权设备批准包。
- 新增训练完成上云触发：手表历史成功落盘后，经已认证 BLE 发送不含业务数据的版本化提示；手机用网络约束唯一 WorkManager 任务同步，BLE/LAN 重连与 15 分钟周期负责补偿。
- Watch Cloud MCP 的允许读取面改为实际设备 projection：OAuth `watch:read` 可读计划名、粗粒度训练、同步状态和训练次数/时长/距离/步数活动健康汇总；睡眠、轨迹、坐标、逐点心率与凭据保持 local-only。
- Watch Cloud MCP 新增 service-binding-only authority observation：独立 capability、完整 audience、严格 vendor media type 与稳定 D1 checkpoint；同 revision 的 truth/时间/hash 不漂移，失败或过期不返回可签名观察。

### Changed

- 手表端全部应用内页面统一重构为 Apple Watch 运动仪表风格：纯黑 AMOLED 画布、跑者图标、状态胶囊、表格大数字和绿/蓝/黄/橙/红运动语义色覆盖主页三屏、计划、准备、训练五屏、确认浮层与历史。
- 跑者图标重绘为粗实心、前倾重心的运动剪影；主按钮、圆形控制和返回操作统一增加 0.94 按压缩放、回弹与触觉，准备页 3-2-1-GO 增加逐拍缩放入场和触觉反馈。
- 手表横向分页改用系统运动应用同类 quintic ease-out、paging touch slop 和短时吸附；页码固定在屏幕底部并随手指连续移动、拉伸，吸附中再次点按也会回到完整页面。
- 训练实时刷新在拖动/吸附期间延期，停稳后只更新当前可见页；隐藏轨迹页不再复制完整坐标数组，主页只在空闲预热相邻静态页。
- 训练轨迹地图改为停稳可见后按需启动、离页暂停；折线与起终点复用并增量追加新点，镜头调整改为 5 秒节流且无逐秒动画。历史详情延迟初始化地图，优先完成首屏指标渲染。
- 训练默认综合仪表按高密度跑步屏重排：计时、距离、当前配速、心率、步频、热量、累计爬升、五区心率条与趋势同屏；平均配速/心率、当前/最高时速、步数和最高心率收敛到相邻数据页，字段与五屏导航保持不变。
- 训练控制统一为黄色暂停、绿色继续和红色结束；准备/主页的开始操作统一为 exercise 绿，计划确认使用绿色主操作，危险操作保持红色层级。
- 手表主页保持原有三屏横向翻页交互逻辑（经用户确认为正确形态，期间的纵向重构已回滚）；信息块新增一行「本周」训练量（距离·次数·活动时间，全零记录不计入）。
- 双端应用图标重绘：深底上三段阶段色圆弧环（跑步绿/快走青/休息黄）加黄色启动三角——间歇训练的视觉直译；升级为自适应图标，任意启动器蒙版下不再裁伤图形。应用名称不变。
- 手机端界面骨架重构：固定底部导航（计划/训练/历史/睡眠）替代埋在滚动流里的标签按钮，内容在导航之下独立滚动；连接设置在已配对后收敛为顶部一行状态（彩色状态点 + 文案），点击展开设置面板，不再让配对表单永久霸占首屏。
- 手机「训练」页由四个无状态按钮重构为实时遥控：每 5 秒读取手表 `/v1/status` 训练实况（状态、活动时间、距离、配速、心率、阶段进度），操作按钮按当前状态渲染（训练中=暂停/结束，已暂停=继续/结束，空闲=开始训练），不再出现按下即报状态错误的按钮。
- 手机端视觉整体重做为数据优先的体锻深色风：纯黑页面、克制的 #1C1C1E 内容卡、白色大数字和活力强调色；移除顶层巨型卡片嵌套，计划列表改用轻量标题、圆形新建入口、描边选中态与降噪后的次要/危险操作，固定底栏增加目的地图形语义并弱化选中背景。
- 手机「训练」页新增体锻式渐变进度环：活动计时嵌在环心，环随计划阶段完成而填充（红粉→橙渐变、圆帽、暗色轨道）。
- 手机计划库升为 schema 3：删除操作与计划库原子保存显式 tombstone；本地读取暂时缺项不再被推断为云端删除，分组与当前选择通过保留的 `sync:library` 密文实体同步。
- 新设备或根密钥变化后先完整 pull 再允许 push；缺少 root 时同步保持未就绪，不再自动生成不兼容密钥。
- BLE pairing secret、LAN credential 与 Gateway API token 首次使用时迁入 Android Keystore AES-GCM 密文；Watch/Phone 均禁用 Auto Backup，Phone 额外排除凭据、同步 state/outbox 和运行时诊断。

### Fixed

- 修复 Android Keystore AES-GCM 在 `randomizedEncryptionRequired=true` 时仍传入调用方 IV、导致真实设备拒绝保存 BLE/LAN/Gateway/device token/root 密文的问题（`BUG-040`）；改为使用 provider-generated IV，Phone 与 Watch 真机回归均已通过。

- 修复加密 V2 只依赖应用启动/周期任务、训练刚结束时云端 MCP 可能暂时没有新记录的问题（`BUG-039`）；安全事件 exact-key/version 负测与唯一任务去重入口已补齐，真实后台/Doze/PC-off 仍按 `WT-025`、`PT-020`、`BLE-011` 验收。

- 恢复历史详情的原始轨迹图；撤销把 `legacy` 记录中的 125m 迁移值当成逐点实测精度、继而隐藏整条路线的错误处理。现有室内测试尚未取得 GNSS fix，不能宣称已达到 35m；系统运动旧轨迹位于签名保护的 `sport_gps` 私有数据链，相关差异继续跟踪（`BUG-038`、`WT-024`）。
- 手表轨迹观察层撤销卫星候选和高德瓦片降级，改接与系统运动同代的 Baidu Map SDK 7.5.9 非卫星矢量底图、本地暗色样式及 SDK 坐标转换；历史地图为 164dp，取景横向 15dp/纵向 25dp，轨迹 3dp。待当前包名/签名 AK 和 OWW221 联网确认（`BUG-037`、`WT-023`）。
- 修复训练综合仪表、训练数据和阶段页采用固定内容高度后在 378×496 屏幕底部留下约 90–100px 黑下巴的问题；内容区现按剩余高度自适应，无心率时显示真实空状态而不绘制假曲线（`BUG-036`、`WT-022`）。
- 修复手表首轮跑者图标关节造型生硬，以及翻页期间隐藏页面刷新、轨迹坐标复制和地图重复工作造成的卡顿风险（`BUG-035`）；OWW221 固定脚本暖态主页 jank 降至 20.10%，三屏往返 14.22%，训练五屏 31.18%，历史轨迹可见性复核通过（`WT-021`）。
- 手机无法读取手表状态时不再显示必然失败的高亮“开始训练”，改为低强调“打开连接设置”（`BUG-030`）。
- 手机无轨迹训练详情不再展示占据大半首屏的空白地图，改为紧凑空状态，让运动概览和详细数据直接进入首屏（`BUG-031`）。

### Documentation

- 新增 Watch 独立 staging 的非空 MCP 验收门禁与 `BUG-041` 台账：OAuth/ready 全绿但没有真实 projection 时必须失败，禁止再把“服务在线”当成“ChatGPT 已读到实际计划与训练”。

## [Watch 0.20.0 / Phone 0.20.0] - 2026-07-26

### Added

- 实时专业跑步指标层 `LiveWorkoutStats`：运动中自动每公里分段（双震动 + 全屏圈卡显示第 N 公里与圈配速）、20 秒滑窗步频、EMA 平滑累计爬升、距离估算千卡、平均/最高心率与五级心率区间。
- 训练主数据页按 Garmin 数据屏重构：黄色大计时 + 当前配速/平均配速、距离/心率双列网格 + 心率五区彩条（当前区间点亮，心率数字同色）。
- 新增「更多数据」页：步频、时速、累计爬升、千卡、步数、最高心率 2×3 网格；训练翻页扩为 控制/主数据/更多数据/阶段环/轨迹 五页。
- 手表 `/v1/status` 在训练进行时返回 `workout` 实时块（状态、阶段、配速、心率区间、步频、爬升、千卡、分段数），经手机透传后 MCP/ChatGPT 可远程查看跑步实况。
- 训练主页以「当前配速」为主读数，分钟/公里与 km/h 同屏显示，并标注数据来源（卫星测速、轨迹推算或步数估算）。
- 速度改为融合 GNSS 多普勒瞬时速度与距离窗口速度：芯片速度新鲜且精度达标时优先使用，失效后自动退回原有窗口估算，并按 4 秒时间常数平滑。
- 手机后台新增 `WatchLanLocator`，用 mDNS 持续发现并校验手表 LAN 端点，不再依赖前台页面里手工填写的 IP。
- 手机新增看门狗：`PhoneBootReceiver` 支持 `WATCHDOG` 动作与精确闹钟，进程被回收后可自行拉起 8766 API。
- 新增 `tools/watch-link.ps1`：按需重建网络 ADB、保持屏幕常亮，并可注册为每 5 分钟运行的计划任务。

### Changed

- 训练主页面按系统运动软件版式重排：顶栏（左阶段/应用名 + 右白色实时时钟）、超大黄色计时为主读数，距离/配速/时速/心率一行一个大数字、单位与标签小字跟随基线；步数移至阶段环页。
- 页边距统一收窄到系统运动的贴边规格；数字全部使用窄体粗体等宽字形。
- 手表滑动逻辑对齐系统习惯：任一 pager 头尾页拖动有阻尼跟随反馈；首页继续右滑退出应用回表盘；训练页不注册退出手势，防止误触结束界面。
- 预备页对齐系统准备页：GPS 状态顶部居中、单一计划标题、发光开始圆；四项就绪状态统一白色。
- 首页与预备页的开始圆增加径向辉光，对齐系统准备页的主按钮质感。
- 训练界面按运动仪表重排：数据全部左对齐显示在纯黑底上，取消卡片框；配速为主读数，时间/距离/心率/步数一行一色（黄/白/红/青），数字统一使用窄体粗体与等宽数字，读数跳动时宽度不变。
- 阶段页改为圆环进度：剩余距离/时间显示在环心，环色跟随阶段（跑步绿、快走青、休息黄）。
- 控制页顶部常驻实时摘要（状态、活动时间、距离与心率），删去无信息量的装饰性标题；暂停为实心主按钮，结束为同色调描边按钮。
- 首页由居中大卡片改为编辑式左对齐排版：计划名为唯一大标题，摘要降为灰阶，仅保留黄色开始按钮一个焦点；删除"向左滑查看"提示文案。
- 预备页四项就绪状态统一为白色，仅警告黄/异常红，不再绿青混排。
- 界面底色改为纯黑，AMOLED 上对应像素不点亮；统一字号刻度与分隔线颜色；页码指示器由文本字形改为真实绘制的圆点。
- 训练页刷新频率由 2 Hz 降为 1 Hz，文本仅在内容变化时写入，轨迹图仅在轨迹页可见时重绘。
- 手表首页在已完成配对后不再常驻显示配对码；训练中心率无读数时显示 `--`，不再以大字显示传感器状态文案。
- 计划阶段列表（首页第三屏与计划页详情）由纯文字拼接行升级为结构化行：阶段色竖条（跑步绿/快走青/休息黄）+ 灰阶序号 + 名称 + 右对齐等宽数字目标，与训练页的阶段语义色一致。
- 预备页 3-2-1-GO 倒计时改用窄体等宽数字字形，与全部训练读数同一字面。
- 历史列表行（完整页与首页速览）的次要信息由「用时 · 步数」改为「用时 · 平均配速 · 心率」，无距离的场次仍回退显示步数；跑步日志按配速扫读，步数留在详情页。
- 历史详情分段卡为最快一公里标记绿色高亮（多于一段时）。
- 手机睡眠列表的整晚时长、深睡与 REM 改为「7小时12分」人读格式，不再使用秒表记法。

### Removed

- 手表端阶段编辑页：计划页重构后编辑入口已撤除，本版删除页面本体；阶段编辑统一在手机端与 MCP 完成。
- 删除旧 `CloudSnapshotPayload` 明文快照生成器；`CloudSnapshotSync` 仅作为旧调用点到加密 V2 客户端的兼容入口，旧 `SYNC_KEY` 不迁移。

### Fixed

- 修复手机后台无法建立 LAN 通道导致 MCP 报 `watch_offline` 的问题（`BUG-020`、`BUG-021`）。
- 修复 8766 监听绑定失败被静默吞掉、无重试也无日志的问题（`BUG-020`）。
- 修复轨迹底图暗色滤镜把空白瓦片渲染成灰蓝色块的问题（`BUG-022`）。
- 修复手机同步把 BLE 当硬前置、LAN 可用仍显示「连接失败：null」且历史/当前计划不读取的问题（`BUG-023`）。
- 修复已配对手机被 mDNS 发现流程误提示「请输入配对码」的问题（`BUG-024`）。
- 修复手表服务被系统回收后 8765/mDNS/BLE 无自愈的问题：`BootReceiver` 精确闹钟看门狗自续（`BUG-025`）。
- 修复手表时长超过 1 小时不进位的问题：训练计时、历史列表与详情统一在 60 分钟后切换 `h:mm:ss`，与手机端一致；历史配速统一为训练页同款 `5'32"` 记法；累计爬升取整显示（`BUG-026`）。
- 修复手机历史详情数据行以硬编码空格排版导致标签值错位的问题：改为标签/值真两列，值加粗右对齐；最佳配速与公里分段统一为概览卡同款 `5:32 /公里` 记法；累计爬升取整（`BUG-027`）。
- 修复手机在 Android 14+ 上同步回调迟到时启动定位前台服务导致的进程崩溃：非前台跳过定位中继启动，服务侧不合规时静默退场（`BUG-028`）。
- 修复手表历史详情从列表进入时分段/最佳配速/心率范围/爬升与轨迹图全部缺失的问题（改为统一全量加载），并修复详情数据行标签被固定值列宽截断为「10 公…」的问题（`BUG-029`）。
- 修复重装/换设备时隐式生成或清除根密钥造成同一云空间不可互解、以及本地缺项误触发远端删除的问题（`BUG-033`）。
- 修复两端 app 自定义 watchdog 与开机广播共用公开 receiver、可被第三方应用反复触发服务启动的问题（`BUG-034`）。

## [Watch 0.19.0 / Phone 0.19.0] - 2026-07-26

### Added

- 手机 Central/GATT Client 与 OWW221 Peripheral/GATT Server 正式替代 debug ping/pong POC，支持分片 request/response、MTU、CCCD indication、自动重连和连接状态。
- 计划 outbox、计划回读与手机定位中继接入 BLE；LAN 保留为历史/睡眠等批量数据的加速链路。
- 手表 BLE 与 8765 LAN 使用共享 `WatchCommandRouter`；手机业务统一经 `WatchConnectionManager` 选择传输。

### Changed

- 手机主页面隐藏手工 IP，BLE 连上后自动同步；IP 仅作为后台 LAN 发现和加速端点。
- 手表和手机版本统一为 0.19.0 debug 候选。

### Fixed

- 修复 Watch MCP 将手机 mDNS IPv6 地址拼成无效 URL、不可达 IPv6 阻塞 IPv4 候选和旧坏缓存持续复用的问题。
- 补齐 Watch MCP OAuth Protected Resource 元数据，Tunnel doctor 可在不暴露 DPAPI Runtime Key 的情况下完成全项检查。
- 修复 checkpoint 写入 offset 前未无条件 flush 缓冲样本的问题。
- 修复 Xiaomi 认证完成后 GATT 写入竞态、Android 13 旧写 API失败，以及 MTU 517 生成 514 字节属性值超过 512 字节上限的问题。
- 修复 BLE 权限缺失且 LAN 可用时每秒重置退避并反复扫描的问题。
- 修复 Xiaomi 对短时间重复 BLE 扫描限流导致第四轮重连超时的问题；首次发现后改用已验证设备直接 GATT 重连。

### Known Issues

- 首次公钥交换、长期密钥、挑战响应、会话加密和防重放已通过 OWW221/Xiaomi 真机验证。
- 无共同 Wi-Fi/无线 ADB、5 分钟息屏、10 次重连、100 次请求和 15 分钟连续运行已通过；双端重启、蓝牙开关恢复、分页续传和非充电功耗门禁尚待执行。

## [Unreleased]

### Added

- 新增独立 `PoyiWatchMcp` 和 `PoyiWatchTunnel` WinSW 服务、Watch 专属端口/DPAPI 数据目录、24 个 `watch_*` 工具及 8 类分页 Resource，不再把 Watch 业务注册到统一 PersonalMcpGateway。
- 手机 8766 新增 Bearer Token、`/v1/health`、`/v1/capabilities`、稳定设备 ID、严格写入元数据和控制命令校验；Watch MCP 仅通过 mDNS 发现手机业务门面。
- 提交 Gradle 8.14.3 Wrapper 和 GitHub Actions，自动测试、lint、构建两端 debug APK，并生成哈希和构建信息。
- 活动训练采用追加式轨迹/心率文件、有界原子检查点；历史改为独立记录目录和摘要索引，并提供详情及 route/heart 分页 API。
- 新增 10 秒平滑当前/最高速度、四类距离来源证据、计划内/自由记录距离及四页手表训练界面。
- 新增手机 mDNS 广播、设备 ID 校验和分层离线错误；独立 Watch Tunnel 只连接本地 Watch MCP。
- 新增计划持久 outbox、operationId、revision、ACK 和删除操作基础协议。
- 手机计划 API 新增 `requestId`、`expectedRevision`、首次结果持久重放与崩溃恢复契约，供独立 Watch MCP 安全重试写入。

### Changed

- Watch MCP 写工具新增 camelCase 参数别名：`requestId`、`expectedRevision`、`commandId`、`expectedState`、`expiresAt`，保持 snake_case 兼容。
- pause/resume 使用明确动作和带前置状态、过期时间的幂等命令，不再映射为 toggle。
- 历史列表只返回摘要，完整样本通过记录详情和游标分页读取。

### Fixed

- 修复独立 Watch MCP WinSW 旧安装保留虚拟服务账户导致 LocalMachine DPAPI/token 文件 ACL 不一致、服务 1067 退出的问题；安装脚本现在强制使用 LocalSystem 并授予 SYSTEM 读取敏感配置。
- 修复安全 BLE 配对后旧 6 位码被清除，手机 `/v1/auth/token` 无法为独立 Watch MCP 签发 token 的问题；现在可使用已配对长期 LAN 凭据完成一次性 bootstrap。
- 修复独立 Watch MCP 在 streamable HTTP stateless 生命周期中关闭全局 HTTP 客户端后，第二次及后续工具调用报 client closed 的问题。
- 修复旧 schema 2 记录缺少新速度/自由记录字段时产生 NaN、阻断整批历史迁移的问题。
- 修复活动进程重建后首页“继续”进入准备页且训练核心页计时显示 00:00 的问题。
- 修复 378×496 首页长要求文本挤压配对码、计划入口和底部安全区的问题。
- 修复检查点后额外 NDJSON 行未进入累计统计却在恢复后继续保留的问题；恢复现在按已确认 offset 截断完整或损坏尾部，并忽略不可解析行计数。
- 修复 Gateway 写计划响应丢失或手机进程在提交后终止时可能重复执行，以及旧 revision 未返回 409 的问题。

### Known Issues

- `0.18.0/0.11.0` 为 debug 候选，尚未完成三次 30–60 分钟户外对比、进程终止矩阵、378×496 全页面截图和功耗测试。
- BLE 已接入计划、控制选择和定位中继；安全配对、防重放、息屏、10 次重连、100 次请求及 15 分钟连续运行已验证，双端重启和真实非充电功耗仍待执行。
- Watch 专属 Tunnel 与同名“步序运动”私人连接已完成绑定，24 个 `watch_*` 工具和读写幂等调用通过；ChatGPT 对 `watch://status` 仍返回 `Unknown resource`，本地 MCP Resource 调用正常。

## [Phone 0.10.1 / MCP 0.5.1] - 2026-07-25

### Fixed

- 修正手机睡眠页遗漏后续 session 的问题；缺失评分和血氧不再显示为 0，MCP 汇总同时返回各指标有效样本数和缺失数。

## [Watch 0.17.0 / Phone 0.10.0 / MCP 0.5.0] - 2026-07-25

### Added

- 手表通过系统 HealthKit Store 读取详细睡眠，手机新增睡眠页，MCP 新增最近睡眠、睡眠列表和睡眠汇总工具。
- 睡眠响应包含评分、血氧、OSA 原值、心率/呼吸范围、多个 session 及完整原始阶段时间线，并明确授权和错误状态。

### Fixed

- MCP `set_training_plan_profile` 现在持久写入手机主计划库并选择、同步、回读校验手表；同步 pending 或两端数据不一致时返回错误，不再产生短暂的手表-only 计划。

### Documentation

- 建立长期项目文档索引、PRD、架构开发规范、测试门禁、缺陷台账和开发决策日志。

## [Watch 0.16.0 / Phone 0.9.0] - 2026-07-24

### Added

- 支持跑步、快走和休息阶段，目标可使用距离或时间。
- 支持默认间歇计划、法特莱克模板和命名/分组多计划库。
- 支持 GPS、步数、心率、总时间、配速、轨迹和数据来源展示。
- 支持训练准备、暂停、继续、停止、自动阶段推进、震动和异常恢复。
- 支持最多 200 条训练历史、完整轨迹、阶段结果和删除。
- 新增手机伴侣应用，提供 mDNS 发现、六位码配对、计划同步、历史详情和手机定位中继。
- 新增本地 MCP，提供计划、训练、统计、历史和轨迹工具。

### Changed

- GPS 未定位不再阻塞训练开始；必要时使用步数估距。
- 原生运动数据按运行时能力探测启用，过期后自动回退。
- 训练数据页和右侧轨迹页改为双向跟手分页。
- 手表界面按 378×496 基准适配并保留底部安全区。

### Known Issues

- 当前为 debug APK 预发布。
- pause/resume 远程接口当前基于 toggle，不具备重试幂等性。
- 当前 OWW221 固件未开放可用的系统户外跑能力映射。
- 自动化测试和 Gradle Wrapper 尚待补齐。

[Unreleased]: https://github.com/666poyi666-collab/WatchIntervals/compare/v0.22.0...HEAD
[Watch 0.22.0 / Phone 0.24.0]: https://github.com/666poyi666-collab/WatchIntervals/releases/tag/v0.22.0
[Watch 0.17.0 / Phone 0.10.0 / MCP 0.5.0]: https://github.com/666poyi666-collab/WatchIntervals/compare/v0.16.0...v0.17.0
[Watch 0.16.0 / Phone 0.9.0]: https://github.com/666poyi666-collab/WatchIntervals/releases/tag/v0.16.0
