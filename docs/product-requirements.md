# 产品需求文档（PRD）

状态：维护中  
基线：2026-08-03
产品：步序（WatchIntervals）

## 1. 产品目标

为 OPPO Watch 4 Pro 提供可独立执行的间歇训练工具，并通过手机伴侣、云端和 Cloud MCP 完成计划管理、训练控制、历史与睡眠回读。核心原则是训练不中断、数据来源透明、云端计划有唯一主版本，且原始轨迹和逐点心率只保留在设备侧。

## 2. 用户与核心场景

| 用户 | 场景 | 成功结果 |
| --- | --- | --- |
| 训练者 | 在手表上选择间歇计划并开始训练 | 无需手机即可完成全部阶段 |
| 训练者 | 户外跑步时查看距离、时间、配速、步数、心率和轨迹 | 指标持续更新且明确数据来源 |
| 训练者 | GPS/厂商能力暂时不可用 | 训练仍可开始，并使用可用传感器降级 |
| 训练者 | 在手机上维护多个命名计划和分组 | 可保存、编辑、选择并同步至手表 |
| 高级用户 | 通过 Cloud MCP 查询、规划与控制训练 | ChatGPT 不依赖电脑即可读取云端真实数据并控制手表训练 |
| 训练者 | 在手机或 MCP 查看手表睡眠 | 经系统授权后读取评分、阶段、血氧、心率和呼吸数据 |

## 3. 功能需求

### 3.1 计划管理

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| REQ-PLAN-001 | 支持跑步、快走、休息三类阶段 | 手机编辑器可创建三类阶段并按顺序保存 | 已实现 |
| REQ-PLAN-002 | 阶段目标支持距离或时间 | 距离以米、时间以秒存储，目标最小值为 1 | 已实现 |
| REQ-PLAN-003 | 内置“1 千米跑 + 200 米快走”计划 | 首次启动或计划损坏时可加载该默认计划 | 已实现 |
| REQ-PLAN-004 | 提供法特莱克模板 | 2 分钟跑/1 分钟走，共 6 组 | 已实现 |
| REQ-PLAN-005 | 计划包含名称、分组、训练要求和阶段列表 | 手机计划库保存后可再次编辑并同步 | 已实现 |
| REQ-PLAN-006 | 云端计划库是多计划管理的主版本，手机保留离线缓存 | 首次 V3 只在空云端接受手机 bootstrap；此后 MCP 和手机写入均使用云端 revision。空计划库与空选择合法：删除最后计划后 Phone/Watch 都为 0；Watch 必须先按收到的 library 清理/生成 selected profile，再提交 library 和 ACK，任一持久化失败均重试且不得启动旧计划 | 正式单设备创建、自动到表和删除往返通过；空库与崩溃窗口自动化通过，待真机空库与多设备验收 |
| REQ-PLAN-007 | 手机计划管理采用列表、详情、编辑三级信息架构 | 列表只承担浏览与进入详情；详情提供阶段顺序、编辑、删除和设为手表当前；编辑草稿支持 Activity 重建恢复和放弃确认，保存不隐式改变手表当前计划；阶段类型、时间/距离切换和移动操作均有明确语义与至少 48dp 触控目标 | Phone 0.24.0 本地实现与自动化通过，待 PT-029 真机 |

### 3.2 训练执行

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| REQ-WORKOUT-001 | 支持准备、开始、暂停、继续、停止、完成状态 | 状态切换后计时、通知和界面一致 | 已实现 |
| REQ-WORKOUT-002 | 达到目标后自动进入下一阶段 | 中间阶段达标后推进；最后阶段标记计划完成并进入自由记录，直到用户手动结束 | 已实现，待户外验证 |
| REQ-WORKOUT-003 | 训练在息屏和离开 Activity 后继续 | 前台服务持有定位、WakeLock/Wi-Fi 条件并持续检查阶段 | 已实现，需持续真机回归 |
| REQ-WORKOUT-004 | 异常终止后恢复活动训练 | 检查点恢复计划、阶段、计时、距离、轨迹、心率样本和阶段结果 | 已实现，需增加自动测试 |
| REQ-WORKOUT-005 | 开始不被搜星阻塞 | 权限满足后可立即开始；GPS 未就绪时展示状态并降级 | 已实现 |
| REQ-WORKOUT-006 | 停止或完成后保存一次历史 | 同一训练 ID 不重复，完成记录包含阶段结果 | 已实现 |
| REQ-WORKOUT-007 | 计划完成与训练会话结束相互独立 | 计划完成后计时、距离、轨迹、步数和心率继续；暂停与恢复保持有效 | 已实现，待真机验证 |
| REQ-WORKOUT-008 | 阶段切换使用短促、非阻塞提示 | 阶段推进由 `WorkoutService` 发出声音/震动反馈；训练页只显示不超过 2 秒、不可聚焦且不拦截手势的轻量提示，不出现需要关闭的窗口 | 本地实现与自动化完成，待 OWW221 WT-026 |
| REQ-WORKOUT-009 | 活动训练在息屏、亮屏和再次打开应用后仍是首要界面 | 只要训练会话仍活动，通知、启动器和任务恢复都直接回到同一 `TrainingActivity`；不得先落到主页或重新创建训练状态，`WorkoutService` 继续作为唯一所有者 | 本地实现与自动化完成，待 OWW221 WT-027 |

### 3.3 数据采集和展示

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| REQ-DATA-001 | 距离优先使用厂商原生运动数据 | 能力探测通过且样本新鲜时使用原生距离 | 已实现 |
| REQ-DATA-002 | 原生数据不可用时使用 GPS | 仅累计精度、速度和时间间隔合理的连续点 | 已实现 |
| REQ-DATA-003 | GPS 不可用时按实际步数估距 | 优先 step counter 差值，缺失时兼容 detector；界面标注来源 | 已实现 |
| REQ-DATA-004 | 展示 GPS 权限、系统定位、卫星和精度状态 | 异常时给出真实状态，不伪造已定位 | 已实现 |
| REQ-DATA-005 | 展示公开或原生心率 | 样本缺失/过期显示“读取中”或“请佩戴”，不显示伪造值 | 已实现 |
| REQ-DATA-006 | 展示总用时、平均配速、实际步数和实时轨迹 | 训练数据页与右侧轨迹页可双向滑动 | 已实现 |
| REQ-DATA-007 | 手机可向手表中继定位 | 用户授权且服务运行时，手机坐标可进入手表训练过滤链路 | 已实现 |
| REQ-DATA-008 | 读取手表系统级睡眠明细 | 使用厂商 HealthKit Store API；返回 session、原始 stage 类型/时间线、评分、血氧、心率和呼吸率 | 已实现，OWW221 真机验证 |
| REQ-DATA-009 | 手机与 MCP 使用同一睡眠数据源 | 手机睡眠页及 MCP 列表、最近记录、汇总均读取手表 `/v1/sleep` | 已实现 |
| REQ-DATA-010 | 长时间轨迹使用追加式存储 | 原始轨迹和心率按样本追加，检查点不重写完整数组；恢复时以检查点 offset 为提交边界，实时地图只使用简化预览 | 已实现，待压力/真机验证 |
| REQ-DATA-011 | 同时展示时速与配速 | 训练页以分钟/公里配速为主读数、km/h 同屏显示，历史展示平均时速、平均配速和最高平滑时速 | 已实现 |
| REQ-DATA-013 | 配速优先使用原生 GNSS 测速 | 芯片多普勒速度新鲜（3 秒内）且速度精度优于 1.6 m/s 时作为主源，失效后退回距离窗口估算；读数按 4 秒时间常数平滑 | 已实现，待户外真机验证 |
| REQ-DATA-014 | 运动中提供专业跑步指标 | 平均配速、每公里自动分段（震动+圈卡）、五级心率区间（彩条+同色数字）、20 秒滑窗步频、EMA 累计爬升、估算千卡、平均/最高心率，全部实时计算并展示 | 已实现，待户外真机验证 |
| REQ-DATA-015 | 训练实况可远程读取 | 手表 `/v1/status` 训练期间返回 `workout` 实时块，手机与 MCP 透传，ChatGPT 可远程查看跑步状态 | 已实现 |
| REQ-DATA-016 | 历史轨迹保留原始证据并明确精度来源 | 旧 schema 的迁移默认精度不得冒充逐点实测值，也不得据此隐藏整条历史路线；原始合法坐标完整展示，地图样式/坐标转换不改写轨迹几何；实时定位能力须以开阔户外实际 fix 验证 | 已恢复历史显示；户外精度与系统私有历史导入仍待验证 |
| REQ-DATA-012 | 保存数据来源证据 | 历史包含各来源距离、轨迹点数、切换记录和定位精度摘要 | 已实现 |
| REQ-DATA-017 | 手机离线保留最后一次成功同步的睡眠 | 手机成功读取最近 31 天睡眠后原子缓存完整 `record/session/stage`；手表断连或暂时读取失败时继续展示缓存及更新时间，失败/空响应不得覆盖有效缓存，损坏缓存安全降级 | 本地实现、自动化与模拟器完成，待 PT-028 真机 |
| REQ-DATA-018 | 手机睡眠页提供可读总览与阶段分布 | 每晚同屏展示评分、总时长、深睡、浅睡、REM、清醒及阶段比例图；多 session 聚合全部数据，系统未提供的指标显示 `--`，不得伪造 0 | 本地实现、自动化与模拟器完成，待 PT-008/028 真机 |
| REQ-DATA-019 | 手机睡眠页保留真实时间顺序并提供周趋势 | 单晚按真实起止时间绘制阶段时间线，保留夜间清醒、多 session 空档和厂商未知阶段；近 7 晚只用已同步缓存绘制总时长趋势，不补造缺失夜晚或阶段 | Phone 0.24.0 本地实现与自动化通过，待 PT-028/030 真机 |

### 3.4 历史和同步

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| REQ-HISTORY-001 | 本地保留最多 200 条训练 | 新记录置顶，超过上限删除最旧记录 | 已实现 |
| REQ-HISTORY-002 | 历史包含时间、距离、步数、心率、计划、阶段结果和轨迹 | 手表/手机/MCP 能读取相同核心字段 | 已实现 |
| REQ-HISTORY-003 | 支持查看详情、轨迹和删除记录 | 删除后列表与 API 都不再返回该记录 | 已实现 |
| REQ-SYNC-001 | 手机自动发现同网手表 | 通过 `_watchintervals._tcp.` mDNS 发现 8765 端口 | 已实现 |
| REQ-SYNC-002 | 使用六位码配对本地 API | 未提供正确 `X-Pairing-Code` 时返回 401 | 已实现 |
| REQ-SYNC-003 | Cloud MCP 支持计划、统计、历史、睡眠和训练控制 | OAuth scope 按 `watch:read`、`watch:write`、`watch:control` 隔离；工具读取 D1 V3 真实数据，不依赖本地 MCP | 正式 OAuth connector 已完成三 scope 授权、真实非空回读和训练删除 |
| REQ-SYNC-004 | ChatGPT 使用 Watch 云端长效 MCP 通道 | 生产链路固定为 `手表 <-> 手机 <-> 云端 <-> Cloud MCP <-> ChatGPT`；电脑关闭不影响读写和控制 | 正式链路已验证；PC 不属于运行时，剩余手机 Doze/重启恢复风险 |
| REQ-SYNC-005 | Windows 本地发现仅作为迁移期兼容能力 | 手机 mDNS/8766 与 Windows MCP/Tunnel 不进入 V3 生产链路；在独立迁移清理批次卸载 | Deprecated，尚未执行卸载 |
| REQ-SYNC-006 | 计划修改支持可靠 BLE/LAN 队列 | 云端计划先原子落手机缓存和可重建 journal；同一 pending 重试保持 operationId，新业务事件（含 A→B→A）使用新 ID，历史 receipt 不得压掉仍存在的不同 pending；完整库删除仍发送 desired-state `upsert`，只兼容读取旧 `delete`；receipt 绑定 Watch+pairing generation，手表全部写入成功后 ACK | 正式 MCP 创建/删除计划已完成云端→手机→手表真机往返；扩大故障路径自动化通过，待 Doze/重启故障注入 |
| REQ-SYNC-007 | MCP 计划写入支持 revision 与请求幂等 | 手机 8766 写计划/选择计划接受 `requestId`、`expectedRevision`；重复请求返回首次结果，复用 ID 或旧 revision 返回 409，崩溃恢复不重复执行 | 已实现，待真机故障注入 |
| REQ-SYNC-008 | 手机与手表以 BLE 作为主控制链路 | 手机 Central/GATT Client 自动连接手表 Peripheral/GATT Server；状态、控制、计划小增量和定位不依赖共同 Wi-Fi | 基础链路已实现，待完整门禁 |
| REQ-SYNC-009 | BLE 与 LAN 共享业务路由并自动选择传输 | 控制、计划和定位优先 BLE；历史/睡眠可使用已验证 LAN 加速；LAN 失败回退 BLE | 已实现基础选择与路由 |
| REQ-SYNC-010 | 蓝牙配对和会话具备应用层安全 | 首次一次性验证码、公钥交换和长期密钥；重连挑战响应；消息认证加密和防重放 | 已实现并通过真机重放验证 |
| REQ-SYNC-011 | 手机在不依赖电脑的情况下同步只读云端快照 | Phone 0.21.1 的 `/sync/push` 快照链路曾通过关机等价测试；迁移后旧路由必须返回 410，旧 `SYNC_KEY` 不得升级为双向设备凭据 | 历史已实现，现已废弃 |
| REQ-SYNC-012 | V2 端到端加密同步只保留迁移回退 | 0.22.x `/sync/v2/exchange` 源码和旧 state 在首次 V3 成功前保留；Phone 0.23.0 只启用 V3，不双写、不自动回退 | Superseded by V3 |
| REQ-SYNC-013 | V3 设备凭据继续由 Android Keystore 包装 | Device token 不以明文 SharedPreferences 保存；业务数据不再做应用层 E2EE，不再依赖同步 root | 已覆盖安装并在正式 Phone exchange 验证 |
| REQ-SYNC-014 | V3 云同步具备后台恢复且不误删数据 | Cloud 与 Watch projection 使用独立 Worker；active request 绑定精确 credential generation，endpoint/device authority 变化重置旧 cursor/request；网络响应的最后凭据复核与全部本地副作用必须在同一凭据锁内完成，换凭据后旧响应不得写库、cursor、receipt 或命令结果；cursor ahead 可重建且冲突保留双方 | 本地实现，自动化通过，待真机 Doze/重启故障注入 |
| REQ-SYNC-015 | 训练完成后由手表主动触发手机上云 | `history_changed` 只提示变化；手机读取不含坐标和逐点心率的 `/v1/history` summary，经 `/sync/v3/exchange` 上传；重连和周期任务补偿 | 正式手表→手机→云端合成分段上行通过；待 Doze/重启补偿回归 |
| REQ-SYNC-016 | 云端永久保存允许的数据并由 MCP 精确回读 | 云端保存完整计划库、训练摘要/分段/平均最低最高心率和睡眠 record/session/stage/评分/血氧/心率/呼吸；原始轨迹、坐标和逐点心率仅在手表/手机 | 正式 ChatGPT 精确回读 1.2 km/2 分段并完成 tombstone 清理；户外实跑只用于传感器真实性 |
| REQ-SYNC-017 | Watch 产品提供统一 V3 authority observation | observation 的 revision/freshness/device state 只来自 V3 checkpoint/device/cursor；旧 `supportsPcOff` 标志不再作为核心云链路门禁 | 正式 Worker 的 storage/OAuth/authority 均 ready，ChatGPT 回读 `cloud_authoritative/fresh` |
| REQ-SYNC-018 | 云端是计划主版本 | 首次 V3 由手机现有库引导空云端；随后所有写入带 expected revision；服务端返回 owner/library 级 `revisionDomainId`，多设备共享同一域，不同 authority 不混水位；Phone 一旦绑定 `v3d.*`，缺字段或 legacy 响应必须 fail closed，仅未绑定 authority 的旧在途响应可一次 fallback | 正式单设备计划库已到表；owner-domain/fail-closed 自动化与正式 Worker 部署通过，待新 APK source 及多设备真机验收 |
| REQ-SYNC-019 | 云端命令 10 秒内确认且离线不迟到执行 | WebSocket 只发送 `sync_needed`；`watch_start_workout(planId)` 必须把目标计划贯穿 Cloud→Phone→Watch 并启动该计划；Watch 在副作用前同步记录 command signature 与解析后的显式 action，`toggle` 首次固定为 pause/resume，重放不得反转；手表 ACK 后同次 exchange 回传，离线保持 pending，30 秒过期后永不执行 | 目标计划与命令 crash-idempotency 自动化通过；正式训练删除及四类控制已有历史真机证据，待新候选抽检 |
| REQ-SYNC-020 | 前台、后台与重连同步必须收敛且不重复占用传输 | 完整同步同一时刻只允许一个 owner；睡眠前台刷新、全量同步和 Worker 共享一次 31 天分页读取；连接恢复后自动补偿，失败 30 秒重试；LAN 的幂等 GET 失败时仅在原 TTL 内建立已认证 BLE 会话并回退，写请求不得自动重放 | Phone 0.24.0 自动化通过，待 PT-030 与 BLE 真机故障注入 |

### 3.5 手表交互和适配

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| REQ-UI-001 | 以 378×496 竖屏为基准适配 | 首页、计划、准备、训练、历史的关键操作不被裁切 | 已实现，需逐版本截图回归 |
| REQ-UI-002 | 保留圆角屏底部安全留白 | 最底部操作在真机可见且可点击 | 已实现 |
| REQ-UI-003 | 横向页面手势跟手且可逆 | 短滑不误切页，左右返回不滞留轨迹页 | 已实现 |
| REQ-UI-004 | 告警只在异常时占用空间 | 正常权限/定位状态不挤压主操作 | 已实现 |
| REQ-UI-005 | 主页保持三屏横向翻页交互 | 首页/历史速览/计划速览横向翻页为用户确认的正确交互形态；改版仅限视觉层，不改变导航结构 | 已确认（2026-07-26 纵向重构经用户否决回滚） |
| REQ-UI-007 | 手表端全部应用内页面采用 Apple Watch 运动仪表视觉体系 | 主页三屏、计划选择、训练准备/倒计时、训练五屏及浮层、历史列表/详情使用同一黑底、高对比运动色、表格数字与圆角组件；主训练屏同屏展示计时、距离、当前配速、心率、步频、热量和爬升，并保留原导航与全部业务状态 | 已实现，378×496 真机静态/室内预检通过，待户外实测数据复核 |
| REQ-UI-008 | 手表交互达到运动场景的连续跟手与明确反馈 | 主页三屏和训练五屏随手指连续位移，释放后以稳定缓动吸附且不会停在半页；固定页码连续反映拖动进度；主操作按压有缩放/触觉反馈，3-2-1-GO 每拍有视觉/触觉反馈；实时刷新和轨迹地图不得在拖动或吸附期间争抢主线程，离开轨迹页后停止地图活动 | 已验证，OWW221 固定脚本与页面回归通过；户外连续 GNSS/心率仍由 WT-005、WT-018 覆盖 |
| REQ-UI-009 | 训练五屏纵向内容适配完整 378×496 画布 | 除有意居中的控制屏外，综合仪表、训练数据、阶段与轨迹的有效内容按剩余高度伸展到底部安全区，不用大块空 View 形成黑下巴；无真实心率时允许文字空状态，但不得伪造曲线 | 已验证，关联 BUG-036、WT-022 |
| REQ-UI-010 | 运动轨迹显示跑道/河岸级地理细节 | 使用非卫星的 Baidu 原生矢量暗色地图，保留河道、堤岸、桥梁等可核对环境，不能只突出国道/高速；轨迹使用细线且完整展示合法坐标，不得伪造或改写路线 | In Progress；Baidu SDK 已接入，待当前包名/签名 AK 和 OWW221 联网验证，关联 BUG-037/038、WT-023/024 |
| REQ-UI-014 | 手表危险操作使用阻断式确认并遵守页面层级返回 | 轻触“结束训练”或“删除记录”只打开确认层，确认前不产生副作用；确认层出现后背景不可触摸且不进入 TalkBack；系统返回、标题返回和右滑先关闭确认层，再按详情到列表、列表到上级退出；服务尚未绑定时结束动作排队到绑定完成，不得静默丢失 | Watch 0.22.0 自动化通过，待 WT-028 真机 |

### 3.6 手机端视觉层级

| 编号 | 需求 | 验收标准 | 状态 |
| --- | --- | --- | --- |
| REQ-UI-006 | 手机端采用数据优先的日光亮色视觉系统 | 保留计划/训练/历史/睡眠四个目的地；浅色内容层不以巨型嵌套卡片和等权按钮堆叠信息，主操作、次操作、危险操作与选中状态有稳定层级 | 本地实现与 API 35 模拟器完成，待 PT-026 真机 |
| REQ-UI-011 | 手机端采用平台中立的分层功能层 | 内容层保持浅色实心数据卡；连接设置与固定底栏使用克制的半透明浮层、细描边和同心圆角；页面使用亮色系统栏、实时安全区与 34sp 大标题，滚动内容可从浮层后方完整滚出 | 本地实现与 API 35 模拟器完成，待 PT-026 真机 |
| REQ-UI-012 | 双端使用一致、原创且可访问的矢量图标体系 | Phone/Watch 启动器共享同一“间歇路线”几何、配色和安全区；四目的地、训练小人及详情返回/定位不依赖 Unicode 或 Apple/SF Symbols；Phone 支持普通、圆形自适应和 monochrome 主题图标，交互图标有中文可访问名称且触控区至少 48dp | 本地实现、跨模块资源测试与模拟器完成，待 PT-027/WT-027 真机 |
| REQ-UI-013 | 手机默认亮色并满足日光可读性 | 首次安装及升级后的默认页面、输入框、卡片、底栏和系统栏均为亮色；正文、次级文字、状态与按钮对比度至少 4.5:1，选中/错误状态不只依赖颜色 | 本地实现、对比度门禁与模拟器完成，待 PT-026 真机 |

## 4. 非功能需求

| 编号 | 要求 | 验收标准 |
| --- | --- | --- |
| REQ-NFR-001 | 数据真实性 | 缺失传感器值不以估造数字伪装；估算值必须标注来源 |
| REQ-NFR-002 | 训练可靠性 | Activity 重建、息屏、短时断网不应终止本地训练 |
| REQ-NFR-003 | 云端主版本、本地可执行 | 云端是计划主版本；手机保留离线缓存；`WorkoutService` 始终是活动训练状态唯一权威 |
| REQ-NFR-004 | 可诊断性 | 关键服务失败应有结构化日志；当前实现不足，关联 `BUG-005` |
| REQ-NFR-005 | 兼容性 | 手表最低 Android 11（API 30）；手机最低 API 29 |
| REQ-NFR-006 | 发布可追溯 | 版本号、提交、APK SHA-256、测试结果和已知问题可追溯 |
| REQ-NFR-007 | 云端受限知情 | 云端可读允许的计划、训练摘要和睡眠明细；不得持有设备私钥、第三方凭据、原始轨迹、坐标或逐点心率；无法验证时返回 unknown/stale |
| REQ-NFR-008 | 本机健康明细不进入系统备份或设备迁移 | `phone_sleep_cache` 及凭据、连接、Cloud state 在 legacy Auto Backup、Android 12+ cloud backup 和 device transfer 中全部显式排除；升级不得因新增缓存文件名漏掉排除规则 |

## 5. 明确边界

- 当前目标设备为 OWW221，不承诺其他 Wear OS/Android 手表的布局和厂商健康接口兼容性。
- BLE 已接入计划 outbox、训练控制和手机定位中继；安全配对、防重放、无共同 Wi-Fi/无线 ADB、5 分钟息屏、10 次重连、100 次请求和 15 分钟连续运行已通过真机验证。双端重启、蓝牙开关恢复、分页续传及非充电状态功耗仍待验证。
- 厂商 HealthKit 能力由运行时探测决定；当前实机固件能力映射为空时走 GPS/步数链路。
- 当前安装的是连接正式 Cloud V3 的 debug 候选包，不等同于正式签名发布 APK。正式 Worker/OAuth/migration、Phone receipt、ChatGPT 非空 MCP 回读、合成公里分段和删除 ACK 已通过；户外 GNSS/心率与手机 Doze/重启仍需真机回归。PC 不属于生产运行时。
- 云同步包含完整计划库、训练摘要/分段/心率统计和睡眠明细；原始轨迹、坐标、逐点心率、设备凭据和诊断日志保持 local-only。
- 应用是训练记录工具，不提供医疗判断。
- 睡眠数据从公开 Store API 读取并按原始 record/session/stage 增量上传云端；暂时读取失败不得推断删除，OSA/血氧等字段只保存系统原值。

## 6. 后续候选需求

| 编号 | 候选项 | 优先级 | 状态 |
| --- | --- | --- | --- |
| REQ-FUTURE-001 | 引入 BLE 发现/兜底通道 | P2 | 已进入 REQ-SYNC-008 至 010 |
| REQ-FUTURE-002 | 导出 GPX/JSON 训练文件 | P2 | 候选 |
| REQ-FUTURE-003 | 自动化单元、协议和状态恢复测试 | P0 | 已进入技术债 |
| REQ-FUTURE-004 | 正式签名、release 构建和升级策略 | P1 | 候选 |
